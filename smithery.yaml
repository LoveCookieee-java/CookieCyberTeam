# Smithery configuration file
# Reference: https://smithery.ai/docs/config

startCommand:
  type: stdio
  config:
    command: python
    args:
      - server.py
      - --stdio
    env:
      CAPE_API_URL: ""
      CAPE_API_KEY: ""
